#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    long long sum = 0;
    for (int i = 0; i < 1000000000; i++) {
        sum++;
    }
    cout << sum << '\n';

    return 0;
}