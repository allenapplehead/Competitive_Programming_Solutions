#include <bits/stdc++.h>
using namespace std;

string solve(string s) {
    string ans;
    // base case
    
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        string s;
        cin >> s;
        cout << solve(s) << '\n';
    }
    return 0;
}