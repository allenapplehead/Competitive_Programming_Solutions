#include <bits/stdc++.h>
using namespace std;

int solve(int w) {
    // base case
    if (w == 1) {
        
    }
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int w;
    cin >> w;
    // find the number of balanced subtrees from top down

    return 0;
}